services:
  - type: web
    name: finsight-api
    env: python
    plan: free
    buildCommand: pip install -r FinSightApp/API/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port=$PORT
    workingDir: FinSightApp/API
